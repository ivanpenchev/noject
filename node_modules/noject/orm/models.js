/**
 * created by: Nikolai Aleksandrenko
 * email: aleksandrenko@gmail.com
 * www: aleksandrenko.com
 */

"use strict";

module.exports = {

    /**
     * Add The model in __models so instanced can be called like this: __models.user.getInstance(...);
     * @param {Object} model
     */
    registerModel: function (modelType, model) {
        this[modelType] = model;
    }
};